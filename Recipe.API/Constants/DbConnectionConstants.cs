namespace Recipe.API.Constants;

public static class DbConnectionConstants
{
    public const string ProductionConnection = nameof(ProductionConnection);
    public const string DebugConnection = nameof(DebugConnection);
}
